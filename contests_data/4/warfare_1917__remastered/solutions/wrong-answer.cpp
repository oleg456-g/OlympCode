#include <bits/stdc++.h>

using namespace std;

#define CNTG 500

void play() {
    vector<int> ord = {6, 6, 6, 6, 6, 6};
    for (int x: ord) {
        cout << x << endl;
        int mv; cin >> mv;
    }
}

int main() {
    for (int i = 0; i < CNTG; ++i)
        play();
}